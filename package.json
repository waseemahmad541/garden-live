{
  "name": "garden-live",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter @garden-live/web dev",
    "build": "pnpm --filter @garden-live/web build",
    "lint": "pnpm --filter @garden-live/web lint",
    "typecheck": "pnpm --filter @garden-live/web typecheck"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "prisma": {
    "schema": "packages/database/prisma/schema.prisma"
  }
}
